# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eksjk_db?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: eksjk
    password: eksjk123
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

# 文件上传配置
eksjk:
  upload:
    # 使用本地文件存储（无需 MinIO）
    storage-type: local
    # 上传文件存储路径（开发环境使用用户目录）
    path: ${user.home}/eksjk-uploads

  # ELTM 外部系统同步地址
  eltm:
    sync-url: http://36.26.56.94:8026/datamain/getpatient

# 日志配置
logging:
  level:
    root: INFO
    com.eksjk: DEBUG
    com.baomidou.mybatisplus: DEBUG